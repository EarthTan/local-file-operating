# 本地文件系统 MCP 服务器 - 安装说明

## 系统要求

- **操作系统**: Windows 10/11, macOS, Linux
- **Python**: 3.10 或更高版本
- **依赖**: MCP Python SDK

## 安装步骤

### 1. 安装 Python

如果您的系统没有 Python，请按以下步骤安装：

#### Windows 用户
1. 访问 [Python 官网](https://www.python.org/downloads/)
2. 下载 Python 3.10+ 安装程序
3. 运行安装程序，**务必勾选 "Add Python to PATH"**
4. 完成安装

#### 验证 Python 安装
打开命令提示符，运行：
```cmd
python --version
```
或
```cmd
py --version
```

应该显示 Python 版本号。

### 2. 安装 MCP SDK

在项目目录中打开命令提示符，运行：
```cmd
pip install mcp[cli]
```

### 3. 启动服务器

#### 方法一：使用批处理文件（推荐）
双击运行 `start_server.bat`

#### 方法二：使用命令行
```cmd
python local_filesystem_mcp.py
```

### 4. 验证服务器运行

成功启动后，您应该看到：
```
启动本地文件系统 MCP 服务器...
服务器将在 http://localhost:8000/mcp 运行
按 Ctrl+C 停止服务器
```

## 故障排除

### Python 相关问题

**问题**: 命令提示符显示 "'python' 不是内部或外部命令"
**解决**: 
1. 重新安装 Python，确保勾选 "Add Python to PATH"
2. 或手动将 Python 添加到系统 PATH 环境变量

**问题**: pip 命令不可用
**解决**:
```cmd
python -m ensurepip
python -m pip install --upgrade pip
```

### MCP 安装问题

**问题**: 无法安装 MCP
**解决**:
```cmd
# 使用国内镜像源（如果网络连接有问题）
pip install mcp[cli] -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 端口冲突

**问题**: 端口 8000 被占用
**解决**: 修改 `local_filesystem_mcp.py` 文件中的端口号：
```python
mcp.run(transport="streamable-http", port=8080)  # 改为其他端口
```

## 测试连接

服务器运行后，可以在浏览器中测试连接：
```
http://localhost:8000/mcp
```

如果看到 JSON 响应，说明服务器正常运行。

## 下一步

完成安装后，请参考 `README_Obsidian_配置指南.md` 配置您的 Obsidian 插件。
